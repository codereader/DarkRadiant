
#ifndef INCLUDE_UTIL_PLATFORM_THREADS_ZTHREADS_HPP
#define INCLUDE_UTIL_PLATFORM_THREADS_ZTHREADS_HPP

// TODO: z-thread headers

namespace Platform {

    namespace Threads {

        // TODO: z-thread type-defs

        /*
        typedef boost::thread thread_t;
        typedef boost::thread_group thread_group_t;

        template<typename T>
        struct thread_specific_ptr_t {
            typedef boost::thread_specific_ptr<T> Val;
        };

        typedef boost::mutex mutex_t;
        typedef boost::recursive_mutex recursive_mutex_t;
        typedef boost::try_mutex try_mutex_t;
        */

    } // namespace Threads

} // namespace Platform

#endif // ! INCLUDE_UTIL_PLATFORM_THREADS_ZTHREADS_HPP
